#ifndef __UTILS_H__
#define __UTILS_H__

#include "common.h"

float pointToSegmentDist(
    const float& px,
    const float& py,
    const float& sx1,
    const float& sy1,
    const float& sx2,
    const float& sy2
);

#endif
